# Database Configuration (REQUIRED)
# Replace with your actual PostgreSQL connection details
DATABASE_URL="postgresql://username:password@localhost:5432/crm_pro_db"

# How to set up your database:
# 1. Install PostgreSQL if not installed
# 2. Create a database: createdb crm_pro_db
# 3. Update the DATABASE_URL above with your credentials
# 4. Run: npx prisma generate
# 5. Run: npx prisma migrate dev --name init

# Authentication (REQUIRED - Generate a secure 32+ character string)
JWT_SECRET="your-super-secret-jwt-key-here-minimum-32-characters-long"

# Cosmic CMS Configuration (REQUIRED for CRM data)
COSMIC_BUCKET_SLUG="your-cosmic-bucket-slug"
COSMIC_READ_KEY="your-cosmic-read-key"
COSMIC_WRITE_KEY="your-cosmic-write-key"

# Email Configuration (Optional)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# Development
NODE_ENV="development"

# Demo Accounts (for reference - created by seed script)
# Admin: admin@crmprodemp.com / password123
# Manager: manager@crmprodemp.com / password123  
# Staff: staff@crmprodemp.com / password123

# PostgreSQL Quick Setup Commands:
# Ubuntu/Debian: sudo apt-get install postgresql postgresql-contrib
# macOS: brew install postgresql
# Windows: Download from https://www.postgresql.org/download/windows/

# Create database and user:
# sudo -u postgres psql
# CREATE DATABASE crm_pro_db;
# CREATE USER crm_user WITH PASSWORD 'your_password';
# GRANT ALL PRIVILEGES ON DATABASE crm_pro_db TO crm_user;
# \q

# Then update DATABASE_URL:
# DATABASE_URL="postgresql://crm_user:your_password@localhost:5432/crm_pro_db"

# Cosmic CMS Setup:
# 1. Sign up at https://www.cosmicjs.com/
# 2. Create a new bucket for your CRM
# 3. Add your bucket slug to COSMIC_BUCKET_SLUG
# 4. Get your read key from Bucket Settings > API Keys
# 5. Get your write key (optional for admin operations)